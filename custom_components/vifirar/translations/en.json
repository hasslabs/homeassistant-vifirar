{
  "config": {
    "step": {
      "user": {
        "title": "Connect your Vi firar site",
        "description": "Your site address (e.g. {exempel}) and the API key from Settings → Home Assistant.",
        "data": {
          "url": "Site address",
          "api_key": "API key"
        }
      },
      "reauth_confirm": {
        "title": "The API key needs renewing",
        "description": "The key for {url} is no longer valid. Create a new one under Settings → Home Assistant on the site and paste it here - your sensors and automations are kept.",
        "data": {
          "api_key": "New API key"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the site - check the address.",
      "invalid_auth": "Wrong API key - generate a new one under Settings → Home Assistant.",
      "insecure_url": "Use https:// - the API key is sent as a Bearer token and would be readable in plain text over http://."
    },
    "abort": {
      "already_configured": "This site is already configured.",
      "reauth_successful": "The key has been updated."
    }
  }
}
